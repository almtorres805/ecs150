#include <stdio.h>
#include <string.h>

int main(int charc, char *argv [])
{
        if (charc < 3){
                printf("Usage: %s [-n] [-c] [-h] [-p PATTERN]... FILE...\n", argv[0]);
        } else {

        }
        return 0;
}
